# 过程日志标准

所有角色执行步骤时必须追加日志到 `log/process.log`。

**格式：** `[{timestamp}] [{角色}] {事件描述}`

**timestamp 获取方式：** 优先使用 `date -u +%Y-%m-%dT%H:%M:%SZ`；如 date 命令不可用，使用递增序号 `#NNN`。

**写入时机：**
- PM 每次调度前、验证后各追加一条
- BA/SA/DE/TE/UX 完成任务后追加一条（含产物路径）
- 人工审批结果追加一条
- 异常/失败追加一条（含原因摘要）

读取 skills/openspec-init.md 的完整内容，严格按照其中定义的步骤执行。

本命令用于为 SnakeShot 项目初始化游戏特性设计规格文档。指定特性时生成对应的规格文件，
写入 SnakeShot/openspec/specs/ 并创建变更记录。

用法: /openspec-init {feature}
当前支持: button-menu (开始/暂停/结束控制菜单)

执行前先读取 CLAUDE.md 确认全局纪律。

# Cline Rules

核心纪律与CLAUDE.md完全一致，本文件仅补充 Cline 环境特有的协议。
> 全局纪律（流程/角色隔离/产物保护/自检/断点恢复/平台适配/产出类型）见 CLAUDE.md

---

## 命令识别

当用户输入以下命令时，读取对应skill文件并严格按步骤执行
| 用户输入 | 读取文件 | 动作 |
|---------|---------|------|
| `/agh-clarify` | skills/agh-clarify.md | 需求初始化与澄清 + 产出类型选择 |
| `/agh-propose` | skills/agh-propose.md | 分析→设计→用例→评估 |
| `/agh-apply` | skills/agh-apply.md | 开发→审计→人工审批 |
| `/agh-archive` | skills/agh-archive.md | 归档+结项 |
| `/agh-run` | skills/agh-run.md | 全流程自动推进 |
| `/agh-android-game` | skills/agh-android-game.md | Android 游戏快捷入口（tech_stack=java/gradle） |
| `/agh-openspec` | skills/agh-openspec.md | 通用 OpenSpec 协议 → 各角色制品版本管理 |
| `/openspec-init` | skills/openspec-init.md | SnakeShot 游戏特性规格初始化 |

---

## 角色切换（Cline 特有）
Cline 环境下无 SubAgent 能力，角色隔离通过以下方式实现：
1. PM 写入 handoff 文件后，输出角色切换指令：`[切换角色: {role}] 请读取{handoff_path}`
2. 切换后严格以目标角色身份执行，仅读取 handoff 白名单文件；3. 禁止引用切换前其他角色的推理内容
4. 完成后输出：`[角色完成: {role}] 输出文件: {path}`，回报PM 角色
5. PM 读取输出文件进行校验，决定下一步
## 辅助 Skill

- DE 编码后执行 skills/dev-test.md（根据tech_stack 路由测试命令）- dev-test 通过后执行 skills/post-verify.md
